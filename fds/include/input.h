#ifndef INPUT_H
#define INPUT_H

#include <iostream>
#include <cmath>
#include <vector>
#include <array>
#include <Eigen/Dense>

enum eQuaternion {e_e0, e_e1, e_e2, e_e3};
enum eEuler {e_phi, e_theta, e_psi};

#define SQ(x) ((x) * (x))

#endif
